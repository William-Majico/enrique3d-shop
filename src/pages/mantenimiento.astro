---
// src/pages/mantenimiento.astro
---

<!DOCTYPE html>
<html lang="es" class="light">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <title>En Mantenimiento | Enrique 3D</title>
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link href="https://cdn.jsdelivr.net/npm/remixicon@3.5.0/fonts/remixicon.css" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            darkMode: 'class',
            theme: {
                extend: {
                    colors: {
                        'warm-cream': '#faf8f5',
                        'warm-terracotta': '#f97316',
                        'discord': '#5865F2',
                    },
                    animation: {
                        'bounce-subtle': 'bounce-subtle 3s ease-in-out infinite',
                        'pulse-slow': 'pulse 4s cubic-bezier(0.4, 0, 0.6, 1) infinite',
                    },
                    keyframes: {
                        'bounce-subtle': {
                            '0%, 100%': { transform: 'translateY(0)' },
                            '50%': { transform: 'translateY(-5px)' },
                        }
                    }
                }
            }
        }
    </script>
    
    <script>
        if (localStorage.theme === 'dark' || (!('theme' in localStorage) && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
            document.documentElement.classList.add('dark');
        } else {
            document.documentElement.classList.remove('dark');
        }
    </script>
    
    <style>
        /* Patrón de cuadrícula para dar efecto de "Blueprint/Constructor" */
        .bg-grid-pattern {
            background-size: 40px 40px;
            background-image: linear-gradient(to right, rgba(0,0,0,0.05) 1px, transparent 1px),
                              linear-gradient(to bottom, rgba(0,0,0,0.05) 1px, transparent 1px);
        }
        .dark .bg-grid-pattern {
            background-image: linear-gradient(to right, rgba(255,255,255,0.05) 1px, transparent 1px),
                              linear-gradient(to bottom, rgba(255,255,255,0.05) 1px, transparent 1px);
        }
    </style>
</head>
<body class="bg-warm-cream dark:bg-[#0f172a] text-gray-800 dark:text-white h-screen w-full flex flex-col items-center justify-center overflow-hidden transition-colors duration-500 relative font-sans bg-grid-pattern">

    <button id="theme-toggle" class="absolute top-6 right-6 z-50 p-3 rounded-full bg-white/80 dark:bg-gray-800/80 backdrop-blur-sm text-gray-800 dark:text-yellow-400 shadow-lg border border-gray-200 dark:border-gray-700 hover:scale-110 transition-all duration-300 focus:outline-none group">
        <i class="ri-sun-fill text-xl hidden dark:block"></i>
        <i class="ri-moon-clear-fill text-xl block dark:hidden text-gray-600 group-hover:text-indigo-500"></i>
    </button>

    <div class="absolute inset-0 overflow-hidden pointer-events-none">
        <div class="absolute top-1/4 left-1/4 w-96 h-96 bg-orange-400/20 dark:bg-orange-600/10 rounded-full blur-[120px] animate-pulse-slow"></div>
        <div class="absolute bottom-1/4 right-1/4 w-96 h-96 bg-indigo-400/20 dark:bg-indigo-600/10 rounded-full blur-[120px] animate-pulse-slow" style="animation-delay: 2s;"></div>
    </div>

    <main class="relative z-10 px-6 text-center max-w-xl mx-auto flex flex-col items-center">
        
        <div class="relative w-32 h-32 mb-12 flex items-center justify-center perspective-container cursor-pointer group">
            <div class="cube">
                <div class="face front"></div>
                <div class="face back"></div>
                <div class="face right"></div>
                <div class="face left"></div>
                <div class="face top"></div>
                <div class="face bottom"></div>
                
                <div class="inner-logo transition-transform duration-500 group-hover:scale-125">
                    <img 
                        src="/favicon.svg" 
                        alt="Logo Enrique 3D" 
                        class="w-14 h-14 object-contain drop-shadow-[0_0_15px_rgba(249,115,22,0.6)] transition-all duration-500 group-hover:drop-shadow-[0_0_30px_rgba(249,115,22,1)]"
                    />
                </div>
            </div>

            <div class="absolute -bottom-8 flex items-center gap-2 bg-white dark:bg-gray-900 px-4 py-1.5 rounded-full shadow-lg border border-gray-200 dark:border-gray-700 animate-bounce-subtle">
                <span class="relative flex h-2.5 w-2.5">
                  <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-orange-400 opacity-75"></span>
                  <span class="relative inline-flex rounded-full h-2.5 w-2.5 bg-orange-500"></span>
                </span>
                <span class="text-xs font-bold text-gray-600 dark:text-gray-300">Construyendo...</span>
            </div>
        </div>

        <h1 class="text-4xl md:text-6xl font-extrabold mb-6 tracking-tight text-gray-900 dark:text-white transition-colors duration-300">
            Estamos <span class="text-transparent bg-clip-text bg-gradient-to-r from-orange-500 to-red-500">Mejorando</span>
        </h1>
        
        <p class="text-lg text-gray-600 dark:text-gray-400 mb-10 leading-relaxed transition-colors duration-300 max-w-md">
            La tienda está en mantenimiento para traerte nuevos assets y una mejor experiencia. ¡Volvemos en unos minutos!
        </p>

        <div class="flex flex-col items-center gap-5 w-full">
            <span class="text-xs font-bold tracking-[0.2em] text-gray-400 uppercase">Mientras tanto en redes</span>
            
            <div class="flex gap-4 justify-center w-full">
                 <a href="https://discord.gg/EbEtTsyrC5" target="_blank" class="group relative flex items-center justify-center gap-2 px-6 py-3 bg-white dark:bg-gray-800 rounded-xl shadow-md hover:shadow-xl hover:-translate-y-1 transition-all duration-300 border border-gray-100 dark:border-gray-700 hover:border-[#5865F2] dark:hover:border-[#5865F2]">
                    <i class="ri-discord-fill text-2xl text-[#5865F2] group-hover:scale-110 transition-transform"></i>
                    <span class="font-bold text-gray-700 dark:text-gray-200 group-hover:text-[#5865F2] transition-colors">Discord</span>
                </a>

                 <a href="https://www.tiktok.com/@enrique_3d" target="_blank" class="group relative flex items-center justify-center gap-2 px-6 py-3 bg-white dark:bg-gray-800 rounded-xl shadow-md hover:shadow-xl hover:-translate-y-1 transition-all duration-300 border border-gray-100 dark:border-gray-700 hover:border-black dark:hover:border-white">
                    <i class="ri-tiktok-fill text-2xl text-gray-900 dark:text-white group-hover:scale-110 transition-transform"></i>
                    <span class="font-bold text-gray-700 dark:text-gray-200 group-hover:text-black dark:group-hover:text-white transition-colors">TikTok</span>
                </a>
            </div>
        </div>

    </main>
    
    <div class="absolute bottom-6 text-sm text-gray-400 opacity-60 font-mono">
        &copy; {new Date().getFullYear()} Enrique 3D Studio
    </div>

    <script>
        // Check cada 5 segundos para no saturar
        setInterval(() => {
            fetch('/', { method: 'HEAD' })
                .then(response => {
                    // Si la URL final ya no es /mantenimiento, recargamos al home
                    if (!response.url.includes('/mantenimiento')) {
                        window.location.href = '/';
                    }
                })
                .catch(() => {});
        }, 5000);
    </script>

    <script>
        const themeToggleBtn = document.getElementById('theme-toggle');
        
        themeToggleBtn!.addEventListener('click', () => {
            const html = document.documentElement;
            
            if (html.classList.contains('dark')) {
                html.classList.remove('dark');
                localStorage.theme = 'light';
            } else {
                html.classList.add('dark');
                localStorage.theme = 'dark';
            }
        });
    </script>

</body>
</html>

<style>
    /* Estilos del Cubo 3D */
    .perspective-container { perspective: 1000px; }
    .cube {
        width: 80px; height: 80px;
        position: relative;
        transform-style: preserve-3d;
        animation: rotateCube 15s infinite linear;
    }
    .perspective-container:hover .cube { animation-play-state: paused; }
    .perspective-container:hover .inner-logo { animation-play-state: paused; }

    .face {
        position: absolute;
        width: 80px; height: 80px;
        background: rgba(249, 115, 22, 0.03); /* Más sutil */
        border: 1px solid rgba(249, 115, 22, 0.4); 
        box-shadow: 0 0 15px rgba(249, 115, 22, 0.05);
        backdrop-filter: blur(2px);
        transition: all 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275);
    }

    /* Posiciones Caras */
    .front  { transform: rotateY(0deg) translateZ(40px); }
    .back   { transform: rotateY(180deg) translateZ(40px); }
    .right  { transform: rotateY(90deg) translateZ(40px); }
    .left   { transform: rotateY(-90deg) translateZ(40px); }
    .top    { transform: rotateX(90deg) translateZ(40px); }
    .bottom { transform: rotateX(-90deg) translateZ(40px); }

    /* Efecto Explosión al Hover */
    .perspective-container:hover .front  { transform: rotateY(0deg) translateZ(80px); }
    .perspective-container:hover .back   { transform: rotateY(180deg) translateZ(80px); }
    .perspective-container:hover .right  { transform: rotateY(90deg) translateZ(80px); }
    .perspective-container:hover .left   { transform: rotateY(-90deg) translateZ(80px); }
    .perspective-container:hover .top    { transform: rotateX(90deg) translateZ(80px); }
    .perspective-container:hover .bottom { transform: rotateX(-90deg) translateZ(80px); }

    .perspective-container:hover .face {
        border-color: rgba(249, 115, 22, 0.8);
        background: rgba(249, 115, 22, 0.1);
        box-shadow: 0 0 30px rgba(249, 115, 22, 0.2);
    }

    .inner-logo {
        position: absolute; top: 50%; left: 50%;
        transform: translate(-50%, -50%);
        animation: rotateCube 15s infinite reverse linear;
    }

    @keyframes rotateCube {
        0%   { transform: rotateX(0deg) rotateY(0deg); }
        100% { transform: rotateX(360deg) rotateY(360deg); }
    }
</style>